/* eslint-disable linebreak-style */
const URL_BACKEND = window.location.href.includes('localhost')
  ? 'http://localhost:8080'
  : 'http://aluraflixhgg.herokuapp.com';

export default {
  URL_BACKEND,
};
